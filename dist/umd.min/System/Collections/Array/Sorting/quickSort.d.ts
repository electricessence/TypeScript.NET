import { Primitive } from "../../../Primitive";
/**
 * Quick sort O(n log (n))
 * Warning: Uses recursion.
 * @param target
 * @returns {T[]}
 */
export declare function quickSort<T extends Primitive>(target: T[]): T[];
